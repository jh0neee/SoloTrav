/**
 * phosphor-react-native 대체 — 같은 아이콘 세트의 웹 판입니다.
 *
 * 두 패키지는 이름(`SirenIcon`)과 props(`size`, `color`, `weight`)가 똑같아서
 * 통째로 넘겨주기만 하면 호출부를 하나도 고칠 필요가 없습니다.
 * 그리는 방식만 다릅니다 — 네이티브는 react-native-svg, 웹은 <svg> 엘리먼트.
 */
export * from '@phosphor-icons/react';
